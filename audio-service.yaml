apiVersion: v1
kind: Service
metadata:
  name: "audio-service"
  labels:
    app: soundunder
spec:
  selector:
    app: soundunder
  ports:
  - port: 80
    targetPort: 8080
    name: "audio-service"
