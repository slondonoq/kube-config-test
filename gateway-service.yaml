apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: soundunder
spec:
  type: LoadBalancer
  selector:
    app: soundunder
  ports:
  - port: 8080
    targetPort: 80
    name: "gateway-service"
